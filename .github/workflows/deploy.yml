name: Build & Deploy

on:
  push:
    branches: [master]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment: ec2
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 8

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - name: Install wasm-pack
        run: cargo install wasm-pack

      - name: Install deps
        run: pnpm install

      - name: Build WASM
        run: |
          wasm-pack build tools/jungle-self-play \
            --target nodejs \
            --out-dir ../../apps/server/src/services/ai/jungle/wasm-pkg \
            --no-default-features \
            --features wasm

      - name: Build
        run: pnpm build

      - name: Pack artifacts
        run: |
          mkdir -p deploy/server/migrations deploy/server/wasm-pkg deploy/server/models deploy/web
          cp apps/server/dist/index.js deploy/server/
          cp -r apps/server/src/services/ai/jungle/wasm-pkg deploy/server/wasm-pkg
          cp -r apps/server/src/db/migrations/* deploy/server/migrations/
          cp apps/server/models/jungle_net.onnx deploy/server/models/
          cp -r apps/web/dist deploy/web/dist
          cd deploy && tar czf ../deploy.tar.gz .

      - name: Upload to EC2
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          source: deploy.tar.gz
          target: /tmp/

      - name: Deploy
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            mkdir -p /home/ubuntu/board-games/server /home/ubuntu/board-games/web
            cd /home/ubuntu/board-games
            tar xzf /tmp/deploy.tar.gz
            rm -f /tmp/deploy.tar.gz
            cd /home/ubuntu/board-games/server
            npm install better-sqlite3 onnxruntime-node
            sudo systemctl restart board-games
